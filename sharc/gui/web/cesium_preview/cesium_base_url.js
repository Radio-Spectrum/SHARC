// Must run before Cesium.js loads. Kept as an external file (not an inline
// <script> block) so it is allowed under a strict CSP without needing
// 'unsafe-inline' in script-src.
window.CESIUM_BASE_URL = "./vendor/cesium/";
